import { bleMonitorService } from '../../core/ble/ble-monitor-service';
const service=bleMonitorService();
Page({data:{state:'CLOSED',deviceId:'-',deviceName:'-',rssi:'-',service:false,notify:false,write:false,firmware:'-',schema:'-',map:'-',diagnostics:{} as any,lastAge:'-',lastError:''},offStore:undefined as undefined|(()=>void),onShow(){this.offStore??=service.store.subscribe(()=>this.refresh());this.refresh()},onHide(){this.offStore?.();this.offStore=undefined},onUnload(){this.offStore?.()},refresh(){const s=service.store,info=s.deviceInfo;this.setData({state:s.state,deviceId:s.deviceId||'-',deviceName:s.deviceName||'-',rssi:s.rssi??'-',service:s.serviceFound,notify:s.notifyFound,write:s.writeFound,firmware:info?`0x${info.firmwareVersion.toString(16).padStart(4,'0')}`:'-',schema:info?.schemaVersion??'-',map:info?`0x${info.registerMapVersion.toString(16).padStart(4,'0')}`:'-',diagnostics:s.diagnostics,lastAge:s.diagnostics.lastDataAt?`${Date.now()-s.diagnostics.lastDataAt} ms`:'-',lastError:s.diagnostics.lastError||''})},copy(){wx.setClipboardData({data:service.store.diagnosticSummary()})},clear(){service.store.clearSessionStatistics()},disconnect(){service.disconnect()}});
