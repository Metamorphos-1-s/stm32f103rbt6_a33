# Run summary

Result: FAIL as 40 Hz evidence. A 75.47 ms configuration-transition interval triggered a threshold intended for steady RUNNING state. The run froze before config readback and before any accepted 40 Hz sample. Automatic restore completed at config `0x0C`. This run led to a diagnostic-only threshold/evidence-retention correction and is not used for rate conclusions.
