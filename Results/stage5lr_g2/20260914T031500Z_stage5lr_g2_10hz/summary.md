# Run summary

Result: FAIL. STM32CubeProgrammer Normal-mode reconnect reset the MCU between separate RAM writes. The control block remained at its boot value and all counters remained zero. This run contains no 10 Hz sampling conclusion. The retained parser output confirms fail-closed handling; later HotPlug evidence is a separate run.
